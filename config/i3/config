####################################
#                				   #
# WatchfulSquids' i3 Configuration #
#			                 	   #
####################################


##############################
### Variables for Settings ######
##############################

# ModKeys
set $mod Mod4
set $smod Shift

# Aethetics
set $wm_font JetBrainsMono NFM, 10
set $wm_inner_gaps 25
set $wm_outer_gaps 0
set $wm_gaps_top 60
set $wm_gaps_bottom 60
set $wm_border_pixel 0
set $wm_smart_gaps off

# Startup Applications
exec --no-startup-id dex --autostart --environment i3
exec_always --no-startup-id ~/.fehbg 
#exec --no-startup-id nitrogen --restore
exec_always --no-startup-id "killall picom; sleep 0.5; picom -b"
exec_always --no-startup-id "sleep 1; ~/.config/xborder/./launch_xborder.sh"
exec_always --no-startup-id ~/.config/polybar/launch_polybar.sh

# Manual Launch Applications
set $wm_terminal kitty
set $wm_browser firefox
set $wm_launcher

################
### Settings ######
################

# Aesthetics
font pango:$wm_font
gaps inner $wm_inner_gaps
gaps outer $wm_outer_gaps
gaps top $wm_gaps_top
gaps bottom $wm_gaps_bottom
default_border pixel $wm_border_pixel
smart_gaps $wm_smart_gaps


################
### Keybinds ######
################

#Launching Applications
bindsym $mod+Return exec kitty
bindsym $mod+$smod+Return exec firefox
bindsym $mod+d exec "rofi -modi drun,run -show drun"
bindsym $mod+$smod+t exec --no-startup-id scrot ~/Pictures/screenshots/`date +%Y-%m-%d_%H:%M:%S`.png
#Window Management
floating_modifier $mod
tiling_drag modifier titlebar
bindsym $mod+Shift+q kill

bindsym $mod+n split h
bindsym $mod+m split v

bindsym $mod+f fullscreen toggle

bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

bindsym $mod+$smod+h move left
bindsym $mod+$smod+j move down
bindsym $mod+$smod+k move up
bindsym $mod+$smod+l move right

bindsym $mod+$smod+u floating toggle
bindsym $mod+u focus mode_toggle
bindsym $mod+y focus parent

bindsym $mod+r mode "resize"

bindsym $mod+$smod+r restart
bindsym $mod+$smod+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

##################
### workspaces ######
##################

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 5 px or 5 ppt
        bindsym k resize grow height 5 px or 5 ppt
        bindsym j resize shrink height 5 px or 5 ppt
        bindsym l resize grow width 5 px or 5 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 20 px or 20 ppt
        bindsym Down resize grow height 20 px or 20 ppt
        bindsym Up resize shrink height 20 px or 20 ppt
        bindsym Right resize grow width 20 px or 20 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

